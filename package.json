{
  "name": "powerbi-backup",
  "version": "1.0.0",
  "description": "Power BI Report Automation Portal — backend (NestJS) + frontend (Angular).",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm --prefix backend run start:dev\" \"npm --prefix frontend start\"",
    "start": "npm run dev",
    "prod": "npm run build && concurrently -n backend,frontend -c blue,green \"npm --prefix backend run start:prod\" \"npx -y serve -s frontend/dist/frontend -p 3018\"",
    "start:prod": "npm run prod",
    "backend": "npm --prefix backend run start:dev",
    "backend:prod": "npm --prefix backend run start:prod",
    "frontend": "npm --prefix frontend start",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "install:all": "npm --prefix backend install && npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
